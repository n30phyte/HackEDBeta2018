syntax = "proto2";

package pongpipe;

message GameInput {
    required bool KeyUp = 1;
    required bool KeyDown = 2;    
}

message GameState {
    enum Pixels {
        paddle1 = 0;
        paddle2 = 1;
        ball = 2;
    }
}
